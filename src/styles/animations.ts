import loading from '../assets/animations/loading/data.json';
import notification from '../assets/animations/notification/data.json';

interface AnimationNames {
  loading: string;
  notification: string;
}
const Animations: AnimationNames = {
  loading,
  notification,
};

export default Animations;
